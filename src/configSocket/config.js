import { api,BASE_URL } from "../../api";

const config = {
    API_URL: api, // Replace with your actual backend URL
    SOCKET_URL: BASE_URL, // Replace with your actual websocket URL
};

export default config;
